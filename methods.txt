blur 

→ what it does: Applies a Gaussian blur to the image.  
→ arguments: radius (optional, defaults to 2).  
→ return value: A modified Pillow Image object.  
→ example: img.blur(5)  
→ before/after image here  

save

→ what it does: Saves the image to the specified file path.  
→ arguments: output_path (required, file name/path). 
→ return value: The original Pillow Image object.  
→ example: img.save("output.jpg")  
→ before/after image here  

grayscale

→ what it does: Converts the image to grayscale ("L" mode).  
→ arguments: None.  
→ return value: A modified Pillow Image object.  
→ example: img.grayscale() 
→ before/after image here  
resize

→ what it does: Resizes the image to the specified width and height.  
→ arguments: w (width), h (height).  
→ return value: A modified Pillow Image object.  
→ example: img.resize(800, 600)  
→ before/after image here  

rotate

→ what it does: Rotates the image by a given angle and expands the bounds to fit.  
→ arguments: angle (required).  
→ return value: A modified Pillow Image object.  
→ example: img.rotate(45)  
→ before/after image here  

invert

→ what it does: Inverts the colors of the image (converts to RGB first).  
→ arguments: None.  
→ return value: A modified Pillow Image object.  
→ example: img.invert()  
→ before/after image here  

brightness

→ what it does: Adjusts the brightness of the image.  
→ arguments: factor (required; 1.0 = normal, 0.5 = dark, 2.0 = bright).  
→ return value: A modified Pillow Image object. 
→ example: img.brightness(1.5)  
→ before/after image here  

pixelate

→ what it does: Pixelates the image using nearest-neighbor interpolation.  
→ arguments: pixel_size (optional, defaults to 10).  
→ return value: A modified Pillow Image object.  
→ example: img.pixelate(20)  
→ before/after image here  

emboss

→ what it does: Applies an emboss filter to the image.  
→ arguments: None.  
→ return value: A modified Pillow Image object.  
→ example: img.emboss()  
→ before/after image here  

contour

→ what it does: Applies a contour filter to the image.  
→ arguments: None.  
→ return value: A modified Pillow Image object.  
→ example: img.contour()  
→ before/after image here  

text

→ what it does: Draws white text on the image at specified coordinates.  
→ arguments: text string (required), x (required), y (required).  
→ return value: A modified Pillow Image object.  
→ example: img.text("Hello", 50, 50)  
→ before/after image here  

show

→ what it does: Displays the image using the default system viewer.  
→ arguments: None.  
→ return value: The original Pillow Image object.  
→ example: img.show() 
→ 
before/after image here  

edges

→ what it does: Applies a filter to find and highlight edges in the image.  
→ arguments: None. 
→ return value: A modified Pillow Image object.  
→ example: img.edges()  
→ before/after image here  

crop

→ what it does: Crops the image to a specified bounding box. 
→ arguments: left (defaults to 0), top (defaults to 0), right (defaults to image width), bottom (defaults to image height).  
→ return value: A modified Pillow Image object.  
→ example: img.crop(10, 10, 200, 200)  
→ before/after image here  

pad

→ what it does: Expands the image with a solid color border.  
→ arguments: pad_val (optional, defaults to 10), color (optional, defaults to "black").  
→ return value: A modified Pillow Image object.  
→ example: img.pad(20, "red")  
→ before/after image here  

canvas

→ what it does: Centers the image on a new canvas of specified dimensions and color.  
→ arguments: w (optional, defaults to image width), h (optional, defaults to image height), color (optional, defaults to transparent).  
→ return value: A modified Pillow Image object.  
→ example: img.canvas(1000, 1000, "white") 
→ before/after image here  

posterize

→ what it does: Posterizes the image by reducing the number of bits for each color channel.  
→ arguments: bits (optional, defaults to 2; range 1 to 8).  
→ return value: A modified Pillow Image object.  
→ example: img.posterize(4)  
→ before/after image here  

solarize

→ what it does: Inverts all pixel values above a given threshold.  
→ arguments: threshold (optional, defaults to 128).  
→ return value: A modified Pillow Image object.  
→ example: img.solarize(150)  
→ before/after image here  

border
→ what it does: Expands the image with a border.  
→ arguments: size (optional, defaults to 10), color (optional, defaults to "black").  
→ return value: A modified Pillow Image object. 
→ example: img.border(15, "blue")  
→ before/after image here  

saturate

→ what it does: Adjusts the color saturation of the image.  
→ arguments: factor (required; float).  
→ return value: A modified Pillow Image object.  
→ example: img.saturate(1.5)  
→ before/after image here  

autocontrast

→ what it does: Maximizes image contrast automatically by calculating the histogram.  
→ arguments: None.  
→ return value: A modified Pillow Image object.  
→ example: img.autocontrast()  
→ before/after image here  

thumbnail

→ what it does: Resizes a copy of the image to fit within a maximum dimension while maintaining aspect ratio.  
→ arguments: max_dim (required; integer).  
→ return value: A modified Pillow Image object.  
→ example: img.thumbnail(250) 
→ before/after image here  

mirror

→ what it does: Creates a symmetrical image by flipping the left side onto the right side. 
→ arguments: None.  
→ return value: A modified Pillow Image object.
→ example: img.mirror()  
→ before/after image here  

3Dialize / threeDialize

→ what it does: Generates and saves a true 3D mesh (.obj) based on the image's height map (grayscale brightness).  
→ arguments: filename (optional, defaults to "model.obj"), depth_scale (optional, defaults to 50.0).  
→ return value: The original Pillow Image object (so the script can keep running).  
→ example: img.3Dialize("mountain.obj", 100.0)  
→ before/after image here  

sketch

→ what it does: Creates a pencil sketch effect using grayscale, inversion, blurring, and Color Dodge blending.  
→ arguments: None.  
→ return value: A modified Pillow Image object. 
→ example: img.sketch()  
→ before/after image here  

glitch

→ what it does: Creates a glitch aesthetic by shifting RGB channels (Red left, Blue right).  
→ arguments: shift_amount (optional, defaults to 15).  
→ return value: A modified Pillow Image object.  
→ example: img.glitch(25)  
→ before/after image here  

ascii

→ what it does: Generates an ASCII art representation of the image and prints it to the console.  
→ arguments: None.  
→ return value: A String containing the ASCII art.  
→ example: img.ascii()  
→ before/after image here  

removeBackground

→ what it does: Uses AI (rembg) to automatically remove the background from the image.  
→ arguments: None. 
→ return value: A modified Pillow Image object.  
→ example: img.removeBackground()  
→ before/after image here  

dominantColor

→ what it does: Calculates the median color of all pixels in the image. 
→ arguments: None.  
→ return value: A tuple representing the RGB dominant color.  
→ example: color = img.dominantColor()  
→ before/after image here  

mosaic

→ what it does: Creates a pixelated mosaic effect while maintaining aspect ratio.  
→ arguments: tiles (optional, defaults to 100).  
→ return value: A modified Pillow Image object.  
→ example: img.mosaic(50)  
→ before/after image here  

pixelSort

→ what it does: Sorts pixels along the x-axis (rows) to create a smeared, glitchy modern art look. 
→ arguments: None.  
→ return value: A modified Pillow Image object.  
→ example: img.pixelSort() 
→ before/after image here  

vaporwave

→ what it does: Maps darks to blue/purple and lights to hot pink, then adds a soft bloom overlay.  
→ arguments: None.  
→ return value: A modified Pillow Image object.  
→ example: img.vaporwave()  
→ before/after image here  

neon

→ what it does: Finds edges, colorizes them cyan, and adds a blurred glow effect.  
→ arguments: None.  
→ return value: A modified Pillow Image object.  
→ example: img.neon()  
→ before/after image here  

hologram

→ what it does: Applies a blue tint, draws horizontal scanlines, and makes the image semi-transparent.  
→ arguments: None.  
→ return value: A modified Pillow Image object.  
→ example: img.hologram()  
→ before/after image here  

kaleidoscope

→ what it does: Mirrors the top-left quadrant of the image into all four corners.  
→ arguments: None. 
→ return value: A modified Pillow Image object.  
→ example: img.kaleidoscope()  
→ before/after image here  

cartoon

→ what it does: Applies a bilateral filter for smoothing and overlays adaptive threshold edges. 
→ arguments: level (optional, defaults to 3).
→ return value: A modified Pillow Image object. 
→ example: img.cartoon(5) 
→ before/after image here  

watercolor

→ what it does: Applies heavy smoothing via a ModeFilter followed by edge enhancement.  
→ arguments: None.  
→ return value: A modified Pillow Image object. 
→ example: img.watercolor()  
→ before/after image here  

oilpaint

→ what it does: Simulates oil painting strokes using Min and Max rank filters.  
→ arguments: None. 
→ return value: A modified Pillow Image object. 
→ example: img.oilpaint() 
→ before/after image here  

histogram

→ what it does: Calculates the histogram data for Red, Green, and Blue channels.
→ arguments: None.  
→ return value: A Python dictionary containing lists for "Red", "Green", and "Blue".
→ example: hist = img.histogram() 
→ before/after image here  

palette

→ what it does: Quantizes the image down to a specified number of colors and extracts the dominant palette.  
→ arguments: num_colors (optional, defaults to 10). 
→ return value: A list of RGB tuples representing the dominant colors.  
→ example: colors = img.palette(5)  
→ before/after image here  

aiUpscale

→ what it does: Doubles the image size using a high-quality Lanczos bicubic approximation resample. 
→ arguments: None.  
→ return value: A modified Pillow Image object. 
→ example: img.aiUpscale() 
→ before/after image here  

detect

→ what it does: Uses Haar cascades to detect objects (faces, eyes, cars) and draws cyan bounding boxes. 
→ arguments: obj_type (optional, defaults to "face"; options include "eye", "car", "vehicle"). 
→ return value: A modified Pillow Image object.
→ example: img.detect("eye") 
→ before/after image here  

lens

→ what it does: Applies a radial Fish-Eye lens distortion. 
→ arguments: strength (optional, defaults to 0.5).  
→ return value: A modified Pillow Image object. 
→ example: img.lens(0.8)  
→ before/after image here  

sphere

→ what it does: Maps the image onto a 3D Sphere projection using displacement.  
→ arguments: None. 
→ return value: A modified Pillow Image object. 
→ example: img.sphere()  
→ before/after image here  

restore

→ what it does: Restores noisy photos while preserving sharp edges using Non-Local Means AI Denoising. 
→ arguments: h_strength (optional, defaults to 10). 
→ return value: A modified Pillow Image object. 
→ example: img.restore(15)  
→ before/after image here  

objectErase

→ what it does: Detects an object (via Haar cascades) and inpaints the background to erase it. 
→ arguments: obj_type (optional, defaults to "face").  
→ return value: A modified Pillow Image object. 
→ example: img.objectErase("face") 
→ before/after image here  

superResolution

→ what it does: Upscales the image using FSRCNN DNN models, gracefully falling back to Lanczos if weights are missing.  
→ arguments: scale_factor (optional, defaults to 2).  
→ return value: A modified Pillow Image object.  
→ example: img.superResolution(4) 
→ before/after image here  

average

→ what it does: Blends 50% of the target image and 50% of another provided image.  
→ arguments: other_img (required; Image object). 
→ return value: A modified Pillow Image object. 
→ example: img.average(img2) 
→ before/after image here  

dct

→ what it does: Generates a normalized visual spectrum of a Discrete Cosine Transform. 
→ arguments: None. 
→ return value: A modified Pillow Image object.  
→ example: img.dct()  
→ before/after image here  

wavelet / dwt

→ what it does: Renders a Discrete Wavelet Transform approximation (LL coefficients using 'haar').  
→ arguments: None. 
→ return value: A modified Pillow Image object. 
→ example: img.wavelet() 
→ before/after image here  

dilate

→ what it does: Applies morphological dilation using a square kernel.  
→ arguments: kernel_size (optional, defaults to 3).
→ return value: A modified Pillow Image object.  
→ example: img.dilate(5) 
→ before/after image here  

erode

→ what it does: Applies morphological erosion using a square kernel. 
→ arguments: kernel_size (optional, defaults to 3). 
→ return value: A modified Pillow Image object. 
→ example: img.erode(5)  
→ before/after image here  

open

→ what it does: Applies morphological opening (erosion followed by dilation).  
→ arguments: kernel_size (optional, defaults to 3). 
→ return value: A modified Pillow Image object.  
→ example: img.open(5) 
→ before/after image here  

close

→ what it does: Applies morphological closing (dilation followed by erosion).  
→ arguments: kernel_size (optional, defaults to 3).  
→ return value: A modified Pillow Image object.  
→ example: img.close(5) 
→ before/after image here  

affine

→ what it does: Applies an affine transform (rotation and translation/shear matrix). 
→ arguments: angle (optional, defaults to 0), tx (optional, defaults to 0), ty (optional, defaults to 0). 
→ return value: A modified Pillow Image object. 
→ example: img.affine(45, 10, 20)  
→ before/after image here  

perspective

→ what it does: Skews the image corners inward by a specified delta for a 4-point perspective warp.  
→ arguments: dx (optional, defaults to 40).  
→ return value: A modified Pillow Image object.  
→ example: img.perspective(50) 
→ before/after image here  

polar

→ what it does: Warps a Cartesian image into a Polar coordinate mapping. 
→ arguments: None. 
→ return value: A modified Pillow Image object. 
→ example: img.polar()  
→ before/after image here  

freqFilter / fftFilter

→ what it does: Applies a Low-pass or High-pass filter in the Frequency (FFT) Domain. 
→ arguments: filter_type (optional, defaults to "lowpass"; options include "highpass"), cutoff (optional, defaults to 30).  
→ return value: A modified Pillow Image object.  
→ example: img.freqFilter("highpass", 40)  
→ before/after image here  

convolve

→ what it does: Applies a specified 2D convolution kernel (e.g., sharpen, edge, box) or a custom matrix.  
→ arguments: kernel_name (optional, defaults to "sharpen"; can also be a custom list array).
→ return value: A modified Pillow Image object.  
→ example: img.convolve("edge")  
→ before/after image here  

graph

→ what it does: Evaluates and plots a mathematical expression (e.g., "x2") directly onto the image space. 
→ arguments: expr (optional, defaults to "x"), color_arg (optional, defaults to "cyan"), thickness (optional, defaults to 2).  
→ return value: A modified Pillow Image object.  
→ example: img.graph("sin(x)", "yellow", 3) 
→ before/after image here  

hsv

→ what it does: Converts the image color space to HSV.  
→ arguments: None. 
→ return value: A modified Pillow Image object. 
→ example: img.hsv()  
→ before/after image here  

lab

→ what it does: Converts the image color space to LAB. 
→ arguments: None. 
→ return value: A modified Pillow Image object. 
→ example: img.lab() 
→ before/after image here  

yuv

→ what it does: Converts the image color space to YUV.  
→ arguments: None.  
→ return value: A modified Pillow Image object. 
→ example: img.yuv()  
→ before/after image here  

xyz

→ what it does: Converts the image color space to XYZ. 
→ arguments: None.  
→ return value: A modified Pillow Image object.
→ example: img.xyz() 
→ before/after image here  

gamma

→ what it does: Applies Gamma correction lookup table adjustments. 
→ arguments: gamma_val (optional, defaults to 2.2).
→ return value: A modified Pillow Image object. 
→ example: img.gamma(1.8)  
→ before/after image here  

linearRGB

→ what it does: Transforms sRGB colors into Linear RGB space. 
→ arguments: None.  
→ return value: A modified Pillow Image object.
→ example: img.linearRGB()  
→ before/after image here  

exposure

→ what it does: Adjusts exposure using EV stops. 
→ arguments: ev (optional, defaults to 1.0). 
→ return value: A modified Pillow Image object. 
→ example: img.exposure(2.0)  
→ before/after image here  

temperature

→ what it does: Adjusts the color temperature (warmth/coolness).
→ arguments: temp (optional, defaults to 20.0).  
→ return value: A modified Pillow Image object. 
→ example: img.temperature(-15.0) 
→ before/after image here  

tint

→ what it does: Adjusts the green/magenta tint shift.  
→ arguments: t_val (optional, defaults to 20.0).
→ return value: A modified Pillow Image object.  
→ example: img.tint(10.0)  
→ before/after image here  

whiteBalance

→ what it does: Applies Gray-World automatic white balance correction.
→ arguments: None.  
→ return value: A modified Pillow Image object.
→ example: img.whiteBalance()  
→ before/after image here  

histogramEqualization

→ what it does: Performs standard global histogram equalization on the Y channel to preserve colors.  
→ arguments: None. 
→ return value: A modified Pillow Image object. 
→ example: img.histogramEqualization() 
→ before/after image here  

CLAHE / clahe

→ what it does: Applies Contrast Limited Adaptive Histogram Equalization to the Lightness channel.  
→ arguments: clip_limit (optional, defaults to 2.0), tile_grid (optional, defaults to 8). 
→ return value: A modified Pillow Image object.  
→ example: img.clahe(3.0, 16)  
→ before/after image here  

toneMap

→ what it does: Applies Filmic Reinhard Tone Mapping.
→ arguments: intensity (optional, defaults to 1.0). 
→ return value: A modified Pillow Image object.
→ example: img.toneMap(1.5) 
→ before/after image here  

mask

→ what it does: Uses a grayscale image as a transparency alpha map to cut out the original image. 
→ arguments: mask_arg (required; an Image object).  
→ return value: A modified Pillow Image object (RGBA). 
→ example: img.mask(mask_img)  
→ before/after image here  

threshold

→ what it does: Applies manual or automatic (Otsu) binary thresholding. 
→ arguments: cutoff (optional, defaults to 128; passing "otsu" enables auto-threshold). 
→ return value: A modified Pillow Image object. 
→ example: img.threshold("otsu") 
→ before/after image here  

magicSelect

→ what it does: Acts like a Photoshop Magic Wand to generate a selection mask via floodfill. 
→ arguments: x (optional, defaults to image center width), y (optional, defaults to image center height), tol (optional, defaults to 32).  
→ return value: A generated selection mask Image object.  
→ example: img.magicSelect(10, 10, 50)  
→ before/after image here 

polygonMask

→ what it does: Generates a white-on-black mask based on provided coordinate points.  
→ arguments: pts (optional, defaults to empty list; accepts nested arrays or a flat list of coordinates). 
→ return value: A generated mask Image object. 
→ example: img.polygonMask([0, 0, 100, 0, 50, 100])  
→ before/after image here  

floodFill

→ what it does: Acts like a Paint Bucket Tool, filling connected pixels with a specified color.  
→ arguments: x (optional, defaults to 0), y (optional, defaults to 0), color_arg (optional, defaults to "red"), tol (optional, defaults to 32).
→ return value: A modified Pillow Image object.  
→ example: img.floodFill(50, 50, "blue", 15) 
→ before/after image here  

alphaMask

→ what it does: Sets the alpha channel directly using another provided image. 
→ arguments: mask_arg (required; an Image object). 
→ return value: A modified Pillow Image object (RGBA). 
→ example: img.alphaMask(mask_img) 
→ before/after image here  

clip

→ what it does: Hard clips pixel intensities between a minimum and maximum value.  
→ arguments: min_val (optional, defaults to 0), max_val (optional, defaults to 255).  
→ return value: A modified Pillow Image object. 
→ example: img.clip(50, 200) 
→ before/after image here  

warp

→ what it does: Applies a 4-point perspective warp based on source and destination coordinates.  
→ arguments: pts1 (source points), pts2 (destination points). 
→ return value: A modified Pillow Image object.  
→ example: img.warp([[0,0], ...], [[10,10], ...])  
→ before/after image here  

meshWarp 

→ what it does: Applies a Flag/Ripple sine wave distortion to the image grid.  
→ arguments: amp (optional, defaults to 10.0), freq (optional, defaults to 0.05). 
→ return value: A modified Pillow Image object.  
→ example: img.meshWarp(15.0, 0.1)  
→ before/after image here  

liquify

→ what it does: Applies a local vector push/smudge (similar to Liquify tools in editors). 
→ arguments: cx (center X), cy (center Y), dx (push X), dy (push Y), radius (optional, defaults to 100.0). 
→ return value: A modified Pillow Image object.  
→ example: img.liquify(250, 250, 10, 15, 50.0)  
→ before/after image here  

swirl

→ what it does: Applies a vortex rotation distortion to the image.  
→ arguments: strength (optional, defaults to 2.0), radius (optional, defaults to min w/h divided by 2), cx (optional, defaults to center X), cy (optional, defaults to center Y).  
→ return value: A modified Pillow Image object. 
→ example: img.swirl(5.0) 
→ before/after image here 

pinch


→ what it does: Applies a Pinch distortion by pulling pixels inward. 
→ arguments: amount (optional, defaults to 0.5), radius (optional, defaults to min w/h divided by 2).  
→ return value: A modified Pillow Image object.  
→ example: img.pinch(0.8)  
→ before/after image here  

bulge

→ what it does: Applies a Bulge distortion by pushing pixels outward.  
→ arguments: amount (optional, defaults to 0.5), radius (optional, defaults to min w/h divided by 2).  
→ return value: A modified Pillow Image object.  
→ example: img.bulge(0.8)  
→ before/after image here  

bandpass

→ what it does: Applies a Frequency Domain Bandpass Filter. 
→ arguments: low_cutoff (optional, defaults to 10), high_cutoff (optional, defaults to 50). 
→ return value: A modified Pillow Image object. 
→ example: img.bandpass(20, 80)  
→ before/after image here 

bandstop

→ what it does: Applies a Frequency Domain Bandstop Filter. 
→ arguments: low_cutoff (optional, defaults to 10), high_cutoff (optional, defaults to 50). 
→ return value: A modified Pillow Image object.  
→ example: img.bandstop(20, 80)  
→ before/after image here  

notch

→ what it does: Applies a Frequency Domain Notch Filter to remove specific cyclic noise. 
→ arguments: u (optional, defaults to 30), v (optional, defaults to 30), r (optional, defaults to 5). 
→ return value: A modified Pillow Image object.  
→ example: img.notch(40, 40, 10) 
→ before/after image here  

frequencyMask

→ what it does: Multiplies a custom visual mask into the image's Frequency Domain. 
→ arguments: mask_arg (required; an Image object).  
→ return value: A modified Pillow Image object.  
→ example: img.frequencyMask(mask_img)  
→ before/after image here  

magnitude

→ what it does: Renders the FFT Magnitude Spectrum of the image.  
→ arguments: None. 
→ return value: A modified Pillow Image object. 
→ example: img.magnitude()  
→ before/after image here  

phase

→ what it does: Renders the FFT Phase Spectrum of the image.  
→ arguments: None. 
→ return value: A modified Pillow Image object. 
→ example: img.phase() 
→ before/after image here  

inverseFFT

→ what it does: Forces an Inverse FFT generation on the image data.  
→ arguments: None.
→ return value: A modified Pillow Image object.  
→ example: img.inverseFFT()  
→ before/after image here  

sobel

→ what it does: Runs Sobel Edge Detection to find horizontal and vertical edge gradients. 
→ arguments: ksize (optional, defaults to 3). 
→ return value: A modified Pillow Image object.
→ example: img.sobel(5)  
→ before/after image here  

scharr

→ what it does: Runs Scharr Edge Detection for higher precision gradient maps.  
→ arguments: None. 
→ return value: A modified Pillow Image object.  
→ example: img.scharr()  
→ before/after image here  

laplacian

→ what it does: Runs Laplacian Edge Detection based on second-derivative gradients.  
→ arguments: ksize (optional, defaults to 3).  
→ return value: A modified Pillow Image object. 
→ example: img.laplacian(5)  
→ before/after image here  

canny

→ what it does: Runs Canny Edge Detection with double-thresholding. 
→ arguments: t1 (optional, defaults to 100), t2 (optional, defaults to 200).  
→ return value: A modified Pillow Image object.
→ example: img.canny(150, 250)  
→ before/after image here  

harris

→ what it does: Runs Harris Corner Detection and overlays points on a visual map.  
→ arguments: block_size (optional, defaults to 2), ksize (optional, defaults to 3), k (optional, defaults to 0.04).  
→ return value: A modified Pillow Image object. 
→ example: img.harris(3, 5, 0.05) 
→ before/after image here  

shiTomasi

→ what it does: Runs Shi-Tomasi Corner Tracking and marks corners with red dots on the image.  
→ arguments: max_corners (optional, defaults to 100), q_level (optional, defaults to 0.01), min_dist (optional, defaults to 10).  
→ return value: A modified Pillow Image object. 
→ example: img.shiTomasi(50, 0.05, 5) 
→ before/after image here  

entropy

→ what it does: Calculates the Shannon Entropy score for the image. 
→ arguments: None.  
→ return value: A float representing the entropy score. 
→ example: score = img.entropy() 
→ before/after image here  

sharpness

→ what it does: Calculates a Sharpness score based on the Variance of the Laplacian.  
→ arguments: None.  
→ return value: A float representing the sharpness score. 
→ example: score = img.sharpness()  
→ before/after image here 

noise

→ what it does: Estimates image noise based on the standard deviation of a Laplacian filter.
→ arguments: None.  
→ return value: A float representing the noise estimate.
→ example: score = img.noise() 
→ before/after image here  

PSNR / psnr

→ what it does: Calculates the Peak Signal-to-Noise Ratio between two images.  
→ arguments: other_img (required; an Image object to compare against). 
→ return value: A float representing the PSNR in decibels (dB).  
→ example: val = img.PSNR(img2)  
→ before/after image here  

SSIM / ssim

→ what it does: Calculates the Structural Similarity Index between two images (requires scikit-image).  
→ arguments: other_img (required; an Image object to compare against).  
→ return value: A float representing the SSIM score.  
→ example: val = img.SSIM(img2)  
→ before/after image here  

objectCount

→ what it does: Counts distinct objects via Connected Components analysis and draws bounding boxes around them.  
→ arguments: None. 
→ return value: A modified Pillow Image object (annotated with boxes).  
→ example: img.objectCount()  
→ before/after image here  

heatmap

→ what it does: Applies an OpenCV Colormap visualization to the image. 
→ arguments: cmap_name (optional, defaults to "JET"; accepts "INFERNO", "VIRIDIS", "HOT", "BONE", "OCEAN").  
→ return value: A modified Pillow Image object. 
→ example: img.heatmap("INFERNO")  
→ before/after image here  

falseColor

→ what it does: Generates a False Color Composite by shifting RGB bands (R gets G, G gets B, B gets R).  
→ arguments: None. 
→ return value: A modified Pillow Image object. 
→ example: img.falseColor()  
→ before/after image here  

depthMap

→ what it does: Calculates a Stereo Disparity Depth Map from a right/left stereo pair.  
→ arguments: other_img (required; an Image object representing the other eye's perspective).  
→ return value: A modified Pillow Image object. 
→ example: img.depthMap(right_eye_img)  
→ before/after image here 

vectorField

→ what it does: Renders a Gradient Vector Field quiver plot with arrows indicating gradient direction. 
→ arguments: step (optional, defaults to 16). 
→ return value: A modified Pillow Image object.
→ example: img.vectorField(32) 
→ before/after image here  

line

→ what it does: Draws a line primitive across the image. 
→ arguments: x1 (optional, defaults to 0), y1 (optional, defaults to 0), x2 (optional, defaults to image width), y2 (optional, defaults to image height), color (optional, defaults to "red"), width (optional, defaults to 2). 
→ return value: A modified Pillow Image object.  
→ example: img.line(10, 10, 100, 100, "blue", 5) 
→ before/after image here  

circle

→ what it does: Draws a circle primitive on the image.  
→ arguments: cx (optional, defaults to center width), cy (optional, defaults to center height), r (optional, defaults to 50), color (optional, defaults to "red"), width (optional, defaults to 2).  
→ return value: A modified Pillow Image object.  
→ example: img.circle(250, 250, 100, "cyan", 3)  
→ before/after image here

rectangle
→ what it does: Draws a rectangle primitive on the image from a top-left coordinate to a bottom-right coordinate.
→ arguments: x1 (optional, defaults to 0), y1 (optional, defaults to 0), x2 (optional, defaults to image width), y2 (optional, defaults to image height), color (optional, defaults to "red"), width (optional, defaults to 2).
→ return value: A modified Pillow Image object.
→ example: img.rectangle(50, 50, 200, 200, "blue", 4)
→ before/after image here

ellipse
→ what it does: Draws an ellipse primitive bounded by the specified box coordinates.
→ arguments: x1 (optional, defaults to 0), y1 (optional, defaults to 0), x2 (optional, defaults to image width), y2 (optional, defaults to image height), color (optional, defaults to "red"), width (optional, defaults to 2).
→ return value: A modified Pillow Image object.
→ example: img.ellipse(10, 10, 150, 200, "green", 3)
→ before/after image here

polygon
→ what it does: Draws a closed polygon outline connecting a provided sequence of point coordinates.
→ arguments: pts (optional, defaults to an empty list), color (optional, defaults to "red"), width (optional, defaults to 2).
→ return value: A modified Pillow Image object.
→ example: img.polygon([(50, 50), (100, 150), (150, 50)], "yellow", 2)
→ before/after image here

arrow
→ what it does: Draws an arrowed line primitive pointing from a starting coordinate to an ending coordinate.
→ arguments: x1 (optional, defaults to 0), y1 (optional, defaults to 0), x2 (optional, defaults to image width), y2 (optional, defaults to image height), color_arg (optional, defaults to "red"), thickness (optional, defaults to 2).
→ return value: A modified Pillow Image object.
→ example: img.arrow(25, 25, 200, 200, "magenta", 5)
→ before/after image here
